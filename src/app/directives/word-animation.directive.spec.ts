import { WordAnimationDirective } from './word-animation.directive';

describe('WordAnimationDirective', () => {
  it('should create an instance', () => {
    const directive = new WordAnimationDirective();
    expect(directive).toBeTruthy();
  });
});
